import React from "react";
import DesignWrapper from "../components/designWrapper";

export default class ExploreRoute extends React.Component {

    render() {
        return (
            <>
                <DesignWrapper>
                    <div>
                        Here should be an input field to search for usernames
                        Straight down here should be some great svg to fill the contentless page with a background
                        It should be something like a colorful paint
                    </div>
                </DesignWrapper>
            </>
        );
    }
}
